# Optional OpenAI SDK for the openai_responses adapter.
# Tranche 3 declares a major bound only. Tranche 4 must pin a tested version
# before request/response translation is accepted.
# Do not install without separate human approval.
openai>=2.0.0,<3.0.0
